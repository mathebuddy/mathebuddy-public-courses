Differentialgleichungen
#######################

---
EXERCISE DGL erster Ordnung mit trennbaren Variablen
@code
let a = rand(2,5);
let y(x) = ode( diff(y,x), - a * x^2/y );
@text
Löse die folgende DGL: $y$:
- $"y"(x)=\pm$ #y
*Hinweis: Geben Sie die Konstante $C \in \R$ mit einem großen `C`, sowie die Wurzel von $x$ mit `sqrt(x)` an.*
---

TODO: migrate old ODE SELL exercises
